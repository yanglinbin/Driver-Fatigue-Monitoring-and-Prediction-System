[project]
name = "guaduation-project"
version = "0.1.0"
description = "Eye blink detection using OpenCV and MediaPipe"
requires-python = ">=3.9"
dependencies = [
    "opencv-python>=4.7.0",
    "mediapipe>=0.10.0",
    "numpy>=1.24.0",
    "matplotlib>=3.7.0",
]
